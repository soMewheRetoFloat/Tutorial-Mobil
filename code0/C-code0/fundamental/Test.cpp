#include <iostream>
#include <stdio.h>
#include <algorithm>
#include <cstring>
using namespace std;
int main()
{
    int a, b;
    char c;
    scanf("%3d %2d %3c", &a, &b, &c);
    printf("%d, %d, %c", a, b, c);
    return 0;
}